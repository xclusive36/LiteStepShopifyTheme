{
  "name": "litestep-theme",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "lint:js": "eslint \"assets/**/*.js\"",
    "test": "jest --runInBand",
    "test:e2e": "npx playwright test",
    "e2e:ci": "MOCK_FALLBACK=1 PREVIEW_URL=\"http://127.0.0.1:9292\" RUN_E2E=1 npx playwright test --project=chromium --reporter=list",
    "preview": "shopify theme dev --store=${SHOPIFY_STORE}",
    "check:ci": "echo \"Theme Check runs in CI (use GitHub Actions).\""
  },
  "devDependencies": {
    "eslint": "^8.57.1",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "@playwright/test": "^1.55.0",
    "axe-core": "^4.10.0"
  }
}
